import styled from 'style'

import Text from './Text'

export const Title = styled(Text).attrs({ as: 'h1', fontSize: ['2rem'] })``

export const Subtitle = styled(Text).attrs({ as: 'h2', fontSize: ['1.5rem'] })``
